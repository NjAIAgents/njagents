{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "//": "Only the tracker is declared by default. Declaring a server the team has not configured makes every install open with failed connections, which teaches people to ignore connection errors.",
  "//adding": "To enable another source, copy its entry from mcp.json.optional into mcpServers here, set the url, add its tool_bindings, and flip the source mode in your team config.",
  "mcpServers": {
    "tracker": {
      "type": "http",
      "url": "https://mcp.atlassian.com/v1/mcp"
    }
  }
}
