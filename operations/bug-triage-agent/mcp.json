{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "//": "Each team authorizes with its OWN credentials, held by the MCP host. No secrets in this repo, ever.",
  "//urls": "Edit the REPLACE_WITH_ urls before enabling a source. These are literal placeholders, not variables: no client expands environment variables in a url field. Only the tracker is needed for the zero-setup tier.",
  "mcpServers": {
    "atlassian": {
      "type": "http",
      "url": "https://mcp.atlassian.com/v1/mcp"
    },
    "datadog": {
      "type": "http",
      "url": "https://REPLACE_WITH_DATADOG_MCP_HOST/mcp"
    },
    "snowflake": {
      "type": "http",
      "url": "https://REPLACE_WITH_SNOWFLAKE_MCP_HOST/mcp"
    },
    "codesearch": {
      "type": "http",
      "url": "https://REPLACE_WITH_CODESEARCH_MCP_HOST/mcp"
    }
  }
}
